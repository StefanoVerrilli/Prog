package Route;

import Model.UserModel.User;
import View.LoginView.LoginAttempT;

public class HomeIncidentFactory implements HomeFactory {

    @Override
    public void createHome(LoginAttempT loginView, User userModel) {


    /*    IncidentView incidentView = new IncidentView();
        MainFrame.getInstance().add(incidentView);
        MainFrame.getInstance().repaint();
        MainFrame.getInstance().revalidate(); */
    }
}
